# ── Required ────────────────────────────────────────────────────────────────
# RCON password for the bundled Minecraft server (docker-compose only)
MC_RCON_PASSWORD=changeme

# ── Optional ─────────────────────────────────────────────────────────────────
# Public URL of the dashboard (default: http://localhost:3001)
# Set this when running behind a reverse proxy or on a non-localhost URL.
# BASE_URL=https://dashboard.example.com

# Set to "true" when running behind an HTTPS reverse proxy
# SECURE_COOKIES=true

# Override auto-generated secrets (not needed in most cases —
# auto-generated on first start and stored in data/.secrets)
# BETTER_AUTH_SECRET=   # openssl rand -base64 32
# ENCRYPTION_KEY=       # openssl rand -hex 32  (must be 64 hex chars)

# Override auto-created admin account (default: admin@localhost with generated password)
# REDSTNE_ADMIN_EMAIL=admin@example.com
# REDSTNE_ADMIN_PASSWORD=your-secure-password

# ── Traefik (docker-compose.traefik.yml only) ─────────────────────────────
# TRAEFIK_DOMAIN=dash.example.com
# TRAEFIK_ACME_EMAIL=you@example.com

# ── Advanced ─────────────────────────────────────────────────────────────────
# PORT=3001
# DB_PATH=data/redstne.db
# NODE_ENV=production

# Auto-seed additional Minecraft servers on startup:
# MC_SERVERS=[{"name":"Survival","host":"mc1","rconPort":25575,"rconPassword":"secret"}]
# or individually:
# MC_SERVER_2_NAME=Creative
# MC_SERVER_2_HOST=mc2
# MC_SERVER_2_PORT=25575
# MC_SERVER_2_PASSWORD=secret
