# Required — generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Required — generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# Initial admin account (created automatically on first start if no admin exists).
# Use *_FILE variants to point to a Docker secret file (like PostgreSQL does).
REDSTNKIT_ADMIN_EMAIL=admin@example.com
REDSTNKIT_ADMIN_PASSWORD=
# REDSTNKIT_ADMIN_EMAIL_FILE=/run/secrets/admin_email
# REDSTNKIT_ADMIN_PASSWORD_FILE=/run/secrets/admin_password
REDSTNKIT_ADMIN_NAME=Admin

# Optional — defaults shown
BASE_URL=http://localhost:3001
CORS_ORIGIN=http://localhost:3001
PORT=3001
DB_PATH=data/redstnkit.db
SERVER_FILES_ROOT=/data/servers
# Set to "true" when running behind an HTTPS reverse proxy
SECURE_COOKIES=false
NODE_ENV=development

# Sample Minecraft server RCON password (docker-compose only)
MC_RCON_PASSWORD=changeme

# ── Auto-seed Minecraft servers ────────────────────────────────────────────
# JSON array of servers to register on first start (upserted by name each restart).
# Useful when running multiple servers on the same Docker network.
#
# Option A — JSON inline:
#   MC_SERVERS=[{"name":"Survival","host":"mc1","rconPort":25575,"rconPassword":"secret"}]
#
# Option B — point to a file (Docker secrets):
#   MC_SERVERS_FILE=/run/secrets/mc_servers
#
# Option C — per-server env vars (up to 9):
#   MC_SERVER_1_NAME=Survival
#   MC_SERVER_1_HOST=mc1
#   MC_SERVER_1_PORT=25575
#   MC_SERVER_1_PASSWORD=secret
#   MC_SERVER_1_DYNMAP=http://mc1:8123      # optional
